import { QuickUserView } from "../share/quick-user-view";

export const metadata = {
  title: "Etsy Quick Optimizer - Listing & Keywords",
  description: "Enter an Etsy listing URL or keyword to instantly receive an optimized title, 13 tags, category, description, and validated keywords.",
};

export default function QuickOptimizePage() {
  return <QuickUserView />;
}
