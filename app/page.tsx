import { QuickUserView } from "./share/quick-user-view";

export const metadata = {
  title: "Etsy Engine | SEO & Competitor Studio",
  description: "Research Etsy competitors, uncover keyword patterns, optimize titles and tags, and plan pricing from one workspace.",
};

export default function HomePage() {
  return <QuickUserView />;
}
