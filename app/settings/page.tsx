import { SettingsView } from "./settings-view";

export const metadata = {
  title: "Settings & Provenance - Etsy SEO Intelligence SaaS",
  description: "Data source hierarchy, freshness policies, scoring algorithm versioning, and Etsy compliance settings.",
};

export default function SettingsPage() {
  return <SettingsView />;
}
